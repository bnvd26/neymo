<form method="post" action="{{ path('admin_users_delete', {'id': userAdmin.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ userAdmin.id) }}">
    <button><img src="{{ asset('/build/delete.svg') }}" alt="Supprimer"></button>
</form>