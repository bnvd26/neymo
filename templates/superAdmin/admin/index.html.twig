{% extends 'base.html.twig' %}
{% block title %}Administrateurs{% endblock %}
{% block content %}
    <div class="content-container">
        <table class="table">
            <thead>
            <tr>
                <th scope="col">ID</th>
                <th scope="col">Email</th>
                <th scope="col">Prénom</th>
                <th scope="col">Nom</th>
                <th scope="col">Gouvernance</th>
                <th scope="col">Role</th>
                <th scope="col">Actions</th>
            </tr>
            </thead>
            <tbody>
            {% for admin in admins %}
            <tr>
                <td>{{ admin.user.id }}</td>
                <td>{{ admin.user.email }}</td>
                <td>{{ admin.firstName }}</td>
                <td>{{ admin.lastName }}</td>
                <td>{{ admin.user.governanceUserInformation.governance.name }}</td>
                <td>{{ admin.role }}</td>
                <td>
                    <a href="{{ path('superadmin_governance_create') }}" class="btn btn-success">Modifier</a> </br>
                    <a href="{{ path('superadmin_governance_create') }}" class="btn btn-success">Supprimer</a>
                </td>
                {% endfor %}
            </tr>
            </tbody>
        </table>
    </div>
{% endblock %}