{% extends 'base.html.twig' %}

{% block title %}GovernanceUserInformation index{% endblock %}

{% block content %}
    <h1>GovernanceUserInformation index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>First_name</th>
                <th>Last_name</th>
                <th>Role</th>
                <th>Gouvernance</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for governance_user_information in governance_user_informations %}
            <tr>
                <td>{{ governance_user_information.id }}</td>
                <td>{{ governance_user_information.firstName }}</td>
                <td>{{ governance_user_information.lastName }}</td>
                <td>{{ governance_user_information.role }}</td>
                <td>{{ governance_user_information.user.governance.name }}</td>
                <td>
                    <a href="{{ path('governance_user_information_show', {'id': governance_user_information.id}) }}">show</a>
                    <a href="{{ path('governance_user_information_edit', {'id': governance_user_information.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('superadmin_user_create') }}">Create new</a>
{% endblock %}
