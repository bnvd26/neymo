{% extends 'base.html.twig' %}

{% block title %}Governance{% endblock %}

{% block content %}
    <h1>Governance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ governance.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ governance.name }}</td>
            </tr>
            <tr>
                <th>Money_name</th>
                <td>{{ governance.moneyName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('governance_index') }}">back to list</a>

    <a href="{{ path('governance_edit', {'id': governance.id}) }}">edit</a>

    {{ include('superAdmin/governance/_delete_form.html.twig') }}
{% endblock %}
