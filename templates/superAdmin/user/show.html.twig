{% extends 'base.html.twig' %}

{% block title %}GovernanceUserInformation{% endblock %}

{% block content %}
    <h1>GovernanceUserInformation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>First_name</th>
                <td>{{ user.firstName }}</td>
            </tr>
            <tr>
                <th>Last_name</th>
                <td>{{ user.lastName }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ user.role }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('governance_admin_information_index') }}">back to list</a>

    <a href="{{ path('governance_admin_information_edit', {'id': user.id}) }}">edit</a>

    {{ include('superAdmin/governanceUserInformation/_delete_form.html.twig') }}
{% endblock %}
